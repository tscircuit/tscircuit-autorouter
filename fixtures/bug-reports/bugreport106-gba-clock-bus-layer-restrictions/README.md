# Native Game Boy clock bus layer restrictions

This is the untouched first-phase SRJ emitted by a fresh native TSCI build of
the full flat Game Boy Advance board. It contains 32 power/clock connections,
all 452 board obstacles, two layers, no prior copper, and blind/buried vias
disabled. Five ordinary clock buses each declare `allowedLayers: ["top"]`.
No failed traces, breakout points, or fabricated obstacles are supplied.

Source: `routing-fully-flat-power-clock-first/phase-0.input.simple-route.json`
from `index.fabrication.fully-flat-power-clock-first.circuit.tsx`.
SHA256: `1f23d882a9bfc142f969560647fa384308221bde2fa5eac0addd4d50096dfb96`.
Producer: tscircuit 0.0.2547, Core 0.0.1906, capacity-autorouter 0.0.900.
The repro branch starts from current autorouter main 61caa467 / 0.0.905.

## Observed native failure

The original native first phase completed with 213 traces and no routing
failure, but two restricted clock connections used bottom copper and vias:

| Complete clock connection | Bus member(s) | Physical length | Vias |
| --- | --- | ---: | ---: |
| U1.XIN to XTAL.pin1, including its shared C_XIN junction | source_trace_66, source_trace_71 | 9.637945 mm | 2 |
| R_XOUT.pin2 to XTAL.pin3 | source_trace_68 | 11.913652 mm | 2 |

Physical lengths include 1.6 mm for each through-via. The TSX requires these
paths to stay below 5 mm with zero vias. This PR specifically reproduces the
ignored TOP bus restriction; SRJ does not carry those TSX maximum-length and
maximum-via limits, so the autorouter-only test is not a test of those limits.
The XIN route must be judged across the complete same-net MST path, not only
its shorter MCU-to-capacitor branch.

The full build later failed LCD_SCK in phase two. That does not explain these
clock detours: they already exist in the phase-zero output, which the next
phase retained byte-for-byte. This fixture reruns only that native first phase
to isolate the bus-layer contract. It is not an accepted whole-board route.

## Where the contract is lost

In latest main 0.0.905, `SimpleRouteBus.allowedLayers` is declared in
`lib/types/srj-types.ts`, but production solvers never read it.
`NetToPointPairsSolver` propagates only `bus.traceWidth`; Pipeline 9 pathing,
high-density routing and simplification receive board-wide layer counts, not
these bus restrictions. Regional high-density repair can also widen nodes to
all board layers. These source observations do not identify which stage first
introduced the observed vias. The existing bus-types test checks metadata
preservation, not routed-layer enforcement.

## Snapshot and validation policy

There is one test and one real repository routing snapshot, generated by
`getBugReportSnapshotSvg` from the solver output in CI. No manually drawn SVG
or saved output copper is committed. The test follows existing bug-report
snapshot conventions and does not assert that today's forbidden bottom
detours must remain; a future fix will naturally change the generated image.
The helper's DRC count is the benchmark evaluator, not complete Core checks,
bus-layer enforcement or fabrication approval. The latest-main routing
outcome is pending the first CI run; the observed lengths above are from the
native 0.0.900 run, not an invented 0.0.905 result.

The test budget is owned by CI. No local solver replay, test suite, benchmark,
production solver change, dependency override or Game Boy source adoption is
part of this repro.
