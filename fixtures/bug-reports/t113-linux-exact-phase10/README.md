# T113-S3 exact phase 10

This fixture is the unmodified `autorouting:start` `simpleRouteJson` emitted by
the real `outputs/t113-linux/index.circuit.tsx` board during its tenth routing
phase. At this point the TSX circuit contains 91 components, 112 preloaded
traces, 24 connections to route, and 387 board obstacles.

The fixture was captured before invoking the autorouter. It preserves the
exact component placement, fanout output, pad connectivity, layer count, and
manufacturing dimensions presented to Pipeline 9.
